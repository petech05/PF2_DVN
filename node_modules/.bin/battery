#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../battery/index.sh" "$@"
  ret=$?
else 
  bash  "$basedir/../battery/index.sh" "$@"
  ret=$?
fi
exit $ret
